// inputs: template id, and any other props specific to the template (eg. name of recipient, etc)
// outputs: email to be sent via amazon SES
// should also have a way to view received emails in browser
// this route should probably work for the above, but if needed another route or a view param to could be used
// if for example, the mime type needs to change or w/e, but that shouldn't happen
// template ideas: new blog post, confirm email, goodbye, hello, custom
